import { Canvas, Meta, Primary, Controls } from "@storybook/blocks";

import * as DropdownStories from "./Dropdown.stories";

<Meta of={DropdownStories} />

# Dropdown

O componente `Dropdown` é um componente de interface que permite ao usuário selecionar uma opção de uma lista de opções. Esta versão possui um componente de `Input` para filtrar as opções.

### Common

<Canvas of={DropdownStories.Common} layout="centered" />

## Documentação Interativa

<Primary />
<Controls />
