import { Meta, Primary, Canvas, Controls } from "@storybook/blocks";

import * as Story from "./Divider.stories.ts";

<Meta of={Story} />

# Divisor

O `Divider` é um componente que exibe uma linha horizontal para separar conteúdos, podendo possuir um texto opcional.

### Divisor Default

<Canvas of={Story.Default} />

### Divisor com Texto

<Canvas of={Story.DivisorWithProps} />

## Documentação Interativa

<Primary />
<Controls />
